-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.approval_requests (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text,
  email text NOT NULL UNIQUE,
  role text NOT NULL,
  department text,
  status text NOT NULL DEFAULT 'pending'::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT approval_requests_pkey PRIMARY KEY (id)
);
CREATE TABLE public.complaints (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  student_id uuid NOT NULL,
  category text NOT NULL,
  description text NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::complaint_status_enum,
  resolved_at timestamp with time zone,
  escalated_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT complaints_pkey PRIMARY KEY (id),
  CONSTRAINT complaints_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.hod_details (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  full_name text,
  email text UNIQUE,
  mobile_number text,
  gender text,
  department text,
  institute text,
  students ARRAY DEFAULT '{}'::uuid[],
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT hod_details_pkey PRIMARY KEY (id),
  CONSTRAINT hod_details_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_directory(id)
);
CREATE TABLE public.leave_requests (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  student_email text NOT NULL,
  student_name text NOT NULL,
  mentor_email text,
  departure_date date NOT NULL,
  return_date date NOT NULL,
  destination text NOT NULL,
  reason text NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text,
  mentor_status text DEFAULT 'pending'::text,
  advisor_status text DEFAULT 'pending'::text,
  hod_status text DEFAULT 'pending'::text,
  approval_chain ARRAY DEFAULT ARRAY['mentor'::text],
  current_approver text DEFAULT 'mentor'::text,
  approved_by text,
  rejected_by text,
  rejection_reason text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT leave_requests_pkey PRIMARY KEY (id)
);
CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid NOT NULL,
  title text NOT NULL,
  message text NOT NULL,
  is_read boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT notifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.outing_requests (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  student_email text NOT NULL,
  student_name text NOT NULL,
  mentor_email text,
  departure_datetime timestamp with time zone NOT NULL,
  return_datetime timestamp with time zone NOT NULL,
  destination text NOT NULL,
  reason text NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text,
  mentor_status text DEFAULT 'pending'::text,
  advisor_status text DEFAULT 'pending'::text,
  hod_status text DEFAULT 'pending'::text,
  approval_chain ARRAY DEFAULT ARRAY['mentor'::text],
  current_approver text DEFAULT 'mentor'::text,
  approved_by text,
  rejected_by text,
  rejection_reason text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT outing_requests_pkey PRIMARY KEY (id)
);
CREATE TABLE public.principal_details (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  full_name text,
  email text UNIQUE,
  institute text,
  mobile_number text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT principal_details_pkey PRIMARY KEY (id),
  CONSTRAINT principal_details_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_directory(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  directory_id uuid UNIQUE,
  email text NOT NULL UNIQUE,
  role USER-DEFINED NOT NULL,
  full_name text NOT NULL,
  mobile_number text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id),
  CONSTRAINT profiles_directory_id_fkey FOREIGN KEY (directory_id) REFERENCES public.user_directory(id)
);
CREATE TABLE public.registration_otp_challenges (
  email text NOT NULL,
  role text NOT NULL DEFAULT 'student'::text,
  otp_hash text NOT NULL,
  otp_salt text NOT NULL,
  expires_at timestamp with time zone NOT NULL,
  verified_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT registration_otp_challenges_pkey PRIMARY KEY (email)
);
CREATE TABLE public.request_approvals (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  request_id uuid,
  approver_id uuid,
  approver_role text NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'approved'::text, 'rejected'::text])),
  reason text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT request_approvals_pkey PRIMARY KEY (id),
  CONSTRAINT request_approvals_request_id_fkey FOREIGN KEY (request_id) REFERENCES public.requests(id),
  CONSTRAINT request_approvals_approver_id_fkey FOREIGN KEY (approver_id) REFERENCES public.user_directory(id)
);
CREATE TABLE public.requests (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  student_id uuid NOT NULL,
  type USER-DEFINED NOT NULL,
  destination text NOT NULL,
  reason text NOT NULL,
  out_datetime timestamp with time zone NOT NULL,
  in_datetime timestamp with time zone NOT NULL,
  status USER-DEFINED NOT NULL DEFAULT 'pending'::request_status_enum,
  current_approver_role USER-DEFINED NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT requests_pkey PRIMARY KEY (id),
  CONSTRAINT requests_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.staff_details (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  full_name text,
  email text UNIQUE,
  mobile_number text,
  gender text,
  department text,
  institute text,
  students ARRAY DEFAULT '{}'::uuid[],
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT staff_details_pkey PRIMARY KEY (id),
  CONSTRAINT staff_details_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_directory(id)
);
CREATE TABLE public.student_details (
  id uuid NOT NULL,
  parent_number text NOT NULL,
  gender USER-DEFINED NOT NULL,
  institute text NOT NULL,
  year integer NOT NULL,
  department text NOT NULL,
  register_number text NOT NULL UNIQUE,
  hostel_block text NOT NULL,
  room_number text NOT NULL,
  mentor_id uuid,
  advisor_id uuid,
  hod_id uuid,
  principal_id uuid,
  CONSTRAINT student_details_pkey PRIMARY KEY (id),
  CONSTRAINT student_details_id_fkey FOREIGN KEY (id) REFERENCES public.profiles(id),
  CONSTRAINT student_details_mentor_id_fkey FOREIGN KEY (mentor_id) REFERENCES public.profiles(id),
  CONSTRAINT student_details_advisor_id_fkey FOREIGN KEY (advisor_id) REFERENCES public.profiles(id),
  CONSTRAINT student_details_hod_id_fkey FOREIGN KEY (hod_id) REFERENCES public.profiles(id),
  CONSTRAINT student_details_principal_id_fkey FOREIGN KEY (principal_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.students_details (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid UNIQUE,
  parent_number text,
  parent_name text,
  class_details text,
  full_name text,
  mobile_number text,
  gender text,
  institute text,
  year text,
  department text,
  register_number text,
  hostel_block text,
  room_number text,
  mentor_id uuid,
  advisor_id uuid,
  hod_id uuid,
  principal_id uuid,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT students_details_pkey PRIMARY KEY (id),
  CONSTRAINT students_details_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_directory(id),
  CONSTRAINT students_details_mentor_id_fkey FOREIGN KEY (mentor_id) REFERENCES public.user_directory(id),
  CONSTRAINT students_details_advisor_id_fkey FOREIGN KEY (advisor_id) REFERENCES public.user_directory(id),
  CONSTRAINT students_details_hod_id_fkey FOREIGN KEY (hod_id) REFERENCES public.user_directory(id),
  CONSTRAINT students_details_principal_id_fkey FOREIGN KEY (principal_id) REFERENCES public.user_directory(id)
);
CREATE TABLE public.user_directory (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  email text NOT NULL UNIQUE,
  role USER-DEFINED NOT NULL DEFAULT 'student'::user_role,
  access_status USER-DEFINED NOT NULL DEFAULT 'pending_approval'::access_status_enum,
  account_status USER-DEFINED NOT NULL DEFAULT 'inactive'::account_status_enum,
  status text NOT NULL DEFAULT 'pending'::text,
  password_created boolean NOT NULL DEFAULT false,
  full_name text,
  register_number text,
  class_details text,
  mobile_number text,
  parent_name text,
  parent_mobile text,
  gender USER-DEFINED,
  institute text,
  year_of_study text,
  department text,
  hostel_block text,
  room_number text,
  onboarding_complete boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  mentor text,
  mentor_email text,
  advisor text,
  advisor_email text,
  hod text,
  hod_email text,
  principal text,
  principal_email text,
  CONSTRAINT user_directory_pkey PRIMARY KEY (id)
);
CREATE TABLE public.warden_details (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  full_name text,
  email text UNIQUE,
  institute text,
  mobile_number text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT warden_details_pkey PRIMARY KEY (id),
  CONSTRAINT warden_details_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.user_directory(id)
);